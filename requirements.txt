# 核心依赖
prefect>=2.14.0
requests>=2.31.0
pydantic>=2.5.0
python-dotenv>=1.0.0
pyyaml>=6.0.1

# 日志
loguru>=0.7.2

# 重试
tenacity>=8.2.3

# 可选依赖（根据需要取消注释）
# 存储
# oss2>=2.18.0  # 阿里云 OSS
# boto3>=1.29.0  # AWS S3

# 通知（邮件使用标准库，钉钉使用 requests）
